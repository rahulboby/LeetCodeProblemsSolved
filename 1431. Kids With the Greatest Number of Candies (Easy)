class Solution(object):
    def kidsWithCandies(self, candies, extraCandies):
        result = []
        maxC = max(candies)
        for i in candies:
            if(i+extraCandies >= maxC):
                result.append(True)
            else:
                result.append(False)

        return result
        
